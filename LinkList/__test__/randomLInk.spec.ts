import { it } from '@jest/globals';
import { getRandomLink } from '../../Util';
it('测试', () => { 




  const node = getRandomLink([[7, null], [13, 0], [11, 4], [10, 2], [1, 0]])
  console.log(node)
})